import React, {useEffect} from "react";
import { ICar } from "../../types/types";

const Cars = (props: {cars: ICar[] }) => {
    

    return (
        <div>
            Cars
        </div>
    )
}

export default Cars;